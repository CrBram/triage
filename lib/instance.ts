/**
 * Hardcoded hospital instance for the dashboard while auth is not wired up.
 * Later this comes from the signed-in account.
 */
export const CURRENT_INSTANCE = {
  id: "az-sint-lucas",
  name: "AZ Sint-Lucas",
} as const;
